еquals() - для сравнения объектов,
hashCode() - для генерации целочисленного кода объекта

Соглашение между equals и hashCode в Java
Если объекты равны по результатам выполнения метода equals, тогда их hashcode должны быть одинаковыми.
Если объекты не равны по результатам выполнения метода equals, тогда их hashcode могут быть как разными,
так и одинаковыми (коллизии).

Согласно контракту методов переопределение equals() должно удовлетвоять требованиям:
* рефлексивность: объект должен равняться себе самому
* симметричность: если a.equals(b) == true, то и b.equals(a) == true
* транзитивность: если a.equals(b) == true и b.equals(c) == true, то a.equals(c) == true
* согласованность: повторный вызов метода equals() должен возвращать одно и тоже значение до тех пор,
  пока какое-либо значение свойств объекта не будет изменено
* неравенство с null: для любого объекта проверка a.equals(null) == false